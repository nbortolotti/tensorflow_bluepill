language: python

python:
  - 3.6

install:
  - pip install codecov
#  - pip install tf-nightly-2.0-preview --quiet
  - pip install tensorflow==2.0.0-alpha0 --quiet

script:
  - python tests/general_checks_test.py
  - coverage run tests/general_checks_test.py

after_success:
  - codecov